- hosts: controller
  become: true
  roles:
    - role: common
      vars:
        python_deps: "{{ controller_python_deps }}"
    - role: code
    - role: rabbitmq
    - role: prometheus
    - role: node_exporter
    - role: selinux
    - role: streamlit